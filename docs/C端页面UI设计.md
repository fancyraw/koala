## C端页面 UI 设计说明

> 视觉基调：**小红书 App 风格** —— 轻盈通透、极致留白、图片即主角、大圆角柔和、UI 弱化让位于内容。红色只作点睛，整体由黑白灰构成高级感。

### 全局设计规范（Design Tokens）

#### 色彩
| 用途 | 色值 | 说明 |
|------|------|------|
| 品牌红 | `#FF2442` | 小红书标志红。仅用于价格、强 CTA、角标、促销标签，是"点睛色"，不大面积使用 |
| 品牌红浅底 | `#FFECEC` | 标签底色（浅红底 + 红字），如「新品」「直降」 |
| 会员金 | 渐变 `#E8C97A → #C9A24B` | 会员等级徽章、会员卡、会员价标识，区别于品牌红，体现"尊享"质感 |
| 主标题 | `#1A1A1A` | 商品名、页面标题、价格数字 |
| 正文 | `#333333` | 常规文字 |
| 次要文字 | `#999999` | 副信息、辅助说明 |
| 占位/弱文字 | `#C8C8C8` | placeholder、未选中态 |
| 页面背景 | `#F8F8F8` | 浅灰背景，让白色卡片"浮起来"，是小红书精致感的关键 |
| 卡片/内容底 | `#FFFFFF` | 所有卡片纯白 |
| 分隔/描边 | `#F0F0F0` | 极浅分割线，能不用就不用，靠留白区分 |

#### 字体
| 层级 | 字号 / 字重 | 用途 |
|------|------------|------|
| 大标题 | 18px / 600 | 页面主标题、商品详情名 |
| 标题 | 15-16px / 600 | 区块标题、卡片商品名 |
| 正文 | 13-14px / 400 | 常规内容 |
| 辅助 | 11-12px / 400 | 已售、副信息 |
| 价格 | 数字 16-26px / 700，¥ 符号缩小到 0.6em | 小红书式价格：符号小、数字大、紧凑 |
| 字体族 | PingFang SC / SF Pro，全程无衬线，绝不用手写体 | — |

#### 形状与质感
| 项目 | 规范 |
|------|------|
| 卡片圆角 | **16px**（大而柔，小红书核心特征），小元素 8-12px |
| 图片圆角 | 商品图 12px；瀑布流/推荐卡片图片为顶部通图，圆角 16px 16px 0 0 |
| 阴影 | 几乎不用硬阴影。卡片靠 `#F8F8F8` 背景与白卡对比浮起；必要时极轻 `0 1px 4px rgba(0,0,0,.04)` |
| 按钮 | 全圆角胶囊（border-radius 999px）。主按钮：品牌红 `#FF2442` 纯色实心 + 白字 + 600；次按钮：`#F5F5F5` 浅灰底 + `#333` 文字 |
| 标签 Pill | 浅色底 + 同色系文字，圆角 6px，小字 10-11px，内边距紧凑（2px 6px） |
| 券标签 | **商品卡图片右上角**的优惠标识。红底白字「券」小标签（圆角 4px，9-10px，距图边约 6px），标示该商品可用满减券。**不使用划线价** |
| 间距 | 基础 8px。模块之间用 **8px 浅灰留白带** 分隔（而非分割线），左右安全边距 12-16px |
| 图标 | 极简线性，2px 描边，风格统一；激活态填充 |

#### 关键氛围词
留白充足、图片大占比、卡片轻浮、红色克制、信息精炼、可"逛"的种草感。

---

### 底部 Tab Bar

```
┌──────────────────────────────────────────────┐
│  🏠首页    🔍发现    🛒购物车(N)    👤我的    │
└──────────────────────────────────────────────┘
```

| 元素 | UI 说明 |
|------|---------|
| 容器 | 高度 50px + 底部安全区，纯白背景，顶部极浅 `#F5F5F5` 1px 线。图标在上文字在下，间距 3px |
| 图标 | 24px 线性，未选中 `#C8C8C8`，选中 `#1A1A1A` 填充态（克制，不用红色） |
| 文字 | 10px，未选中 `#C8C8C8`，选中 `#1A1A1A` 600 |
| 购物车角标 | 图标右上角品牌红 `#FF2442` 圆形气泡，白字，直径 15px，超过 99 显示「99+」 |

---

#### 首页

```
┌──────────────────────────────────────┐
│ 顶部：Logo + 品牌名                       │
├──────────────────────────────────────┤
│ Banner 轮播区（纯图，无标签）            │
├──────────────────────────────────────┤
│ 品类专区（图标网格）                      │
├──────────────────────────────────────┤
│ 热销爆款（横向滑动卡片列表 + 查看全部）   │
├──────────────────────────────────────┤
│ 精选推荐（左右卡：图左文右屏效高）          │
└──────────────────────────────────────┘

﹝叠加﹞新人券弹窗：新用户注册后首次进入首页自动弹出
```

| 模块 | UI 说明 |
|------|---------|
| 页面背景 | `#F8F8F8` 浅灰，所有内容区为白色，模块间 8px 灰留白带 |
| 顶部导航 | 高度 48px，白色背景，左对齐简洁。左侧品牌 Logo（圆角方形 28px，红色）+ 紧跟品牌名 `#1A1A1A` 17px 600。**不放搜索框和消息图标**，保持轻盈 |
| Banner 轮播区 | 白色容器内，左右边距 12px，圆角 16px。**纯图片轮播，不叠加任何标签角标**，让主视觉干净。底部居中指示器：当前页白色长条（宽 14px 高 3px 圆角），其余白色半透明小圆点。点击跳转商品列表/活动页 |
| 品类专区 | 白色容器。一行 5 个品类均分：每个为 **48px 圆角方形**（圆角 14px）浅彩底（每类不同的极浅马卡龙色），内置线性图标，下方品类名 11px `#333`，间距 6px |
| 热销爆款 | 标题「🔥 热销爆款」`#1A1A1A` 16px 600 + 右「查看全部 ›」`#999` 13px。横向滑动卡片，露出 2.4 张。卡片宽 130px 圆角 16px 白底：顶部 1:1 通图，**左上角「爆款」红底白字标签贴角，右上角红底白字「券」标签**。信息区 padding 10px：商品名 13px 单行省略，价格行 `¥`12px 红 + `88`18px 红 700 + **右侧「已售 N」11px `#999`**。点击整卡进详情 |
| 精选推荐（左右图文卡） | **首页核心种草位**。标题「✨ 为你推荐」。**单列左右布局卡**（图左信息右，提升屏效），卡片间距 12px，圆角 16px 白底。每张卡片为横向 flex 布局：① 左侧方图（1:1 正方，宽约 120px，圆角 12px，图片仍大占比）**右上角红底白字「券」标签**；② 右侧信息区 padding 12px 14px，纵向排列：商品名 15px 600 `#1A1A1A` 单行；**推荐语**（核心）——2 行卖点文案 13px `#666`，行高 19px，如「编辑精选 · 通勤好物，颜值与实力兼具」；底部行：左价格 `¥68` 红 17px 700，右侧小字「128 人已购」`#999`。左图右文紧凑排布，一屏展示更多商品，屏效更高 |
| 新人券弹窗 | 新用户注册后首次进首页自动弹出。居中模态弹窗（宽约 300px，圆角 20px）：顶部红→金渐变区 + 大字「🎁 新人专享」+「¥5 无门槛券」白字超大数字；中部「注册好礼，下单立减」13px `#666`；底部「立即领取」品牌红实心大胶囊按钮。右上角/底部关闭 ✕。背景半透明黑遮罩。这是无门槛券的唯一领取入口 |

---

#### 商品列表页

```
┌──────────────────────────────────────┐
│ 顶部：返回 + 品类名                       │
├──────────────────────────────────────┤
│ 品类筛选栏（横向可滑动，选中高亮）        │
├──────────────────────────────────────┤
│ 商品瀑布流（双列，种草卡片）             │
└──────────────────────────────────────┘
```

| 模块 | UI 说明 |
|------|---------|
| 页面背景 | `#F8F8F8` |
| 顶部导航 | 高度 48px 白底。左侧返回箭头 `#1A1A1A`，中间标题为当前品类名或「全部商品」16px `#1A1A1A`。**无搜索框** |
| 品类筛选栏 | 白底横向滑动。文字标签：未选中 `#999` 13px，选中 `#1A1A1A` 14px 600 + 下方 3px 红色圆角短指示条（`#FF2442`，宽 16px）。标签间距 22px |
| 商品瀑布流 | 双列瀑布流，列间距 8px，左右边距 8px。卡片圆角 16px 白底：① 顶部商品图（**高度不等**制造瀑布感，圆角 16px 16px 0 0）**右上角红底白字「券」标签**；② 信息区 padding 10px：商品名 13px `#1A1A1A` 最多两行；③ 价格行 `¥88` 红 15px 700 + 右侧「已售 N」11px `#999`。**无排序栏、无划线价**，纯靠品类筛选 + 瀑布流浏览 |
| 空状态 | 居中：极简线性插画（空盒）80px `#E0E0E0` + 「这里还没有好物～」13px `#999` + 下方「随便逛逛」红色文字按钮 |

---

#### 商品详情页

```
┌──────────────────────────────────────┐
│ 悬浮导航：返回 + 分享（毛玻璃圆钮）       │
├──────────────────────────────────────┤
│ 商品主图轮播（全宽大图，1:1）            │
├──────────────────────────────────────┤
│ 价格区：现价 + 券标签 + 会员价            │
│ 商品名称 + 已售数量                      │
│ 优惠提示：满100减10 可用                  │
├──────────────────────────────────────┤
│ 选择规格 / 购买数量                       │
├──────────────────────────────────────┤
│ 产品亮点 / 详情长图                       │
├──────────────────────────────────────┤
│ 底部栏：客服 + 加购 + 立即购买            │
└──────────────────────────────────────┘
```

| 模块 | UI 说明 |
|------|---------|
| 页面背景 | `#F8F8F8`，内容白卡分段，段间 8px 灰带 |
| 悬浮导航 | 浮于主图上，初始透明，下滑渐变白底。返回/分享为 32px 圆形毛玻璃钮（`rgba(255,255,255,.85)` + blur + 细描边），图标深色 |
| 商品主图 | 全宽 1:1 大图直铺到边，无圆角。底部居中白色长条指示器。右下角可叠加「1/5」半透明黑底白字小胶囊 |
| 价格区 | 白卡 padding 16px。现价 `¥`14px 红 + `88`28px 红 700。右侧「券 满100减10」标签——浅红底 `#FFECEC` 红字 10px 圆角 6px。价格下方一行**会员价提示**：金色「🥈 白银会员 ¥86.24」小字（会员金 11px），非会员则显示「开通会员享 9.8 折 ›」引导。**不展示划线价** |
| 商品名称 | `#1A1A1A` 17px 600，行高 24px，最多两行 |
| 已售数量 | 「已售 2,680」`#999` 12px（**不展示收藏数**） |
| 优惠提示 | 浅红卡片 `#FFF5F5` 圆角 10px，左侧红「券」方标，中间「满100减10 可用」`#333` 13px，右「领取」红 13px。即使不手动领，下单时购物车也会自动领取使用 |
| 规格选择 | 白卡。标题「规格」15px 600。规格 pill 横排：未选 `#F5F5F5` 底 `#333`，选中 `#FFECEC` 浅红底 + `#FF2442` 红字 + 红描边 1px。圆角 8px padding 8px 16px |
| 数量步进 | 行内：左「数量」15px，右步进器「− 1 +」，按钮 28px 圆形 `#F5F5F5` 底 |
| 产品亮点 | 白卡，3-4 个亮点横排：圆角方形 `#FAFAFA` 底图标 44px + 下方文字 11px `#666` |
| 详情长图 | 全宽长图图文，白底留白充足 |
| 底部操作栏 | 白底高 56px+安全区。左侧「客服」竖排小图标（44px，图标+8px小字 `#999`），右侧两个胶囊按钮：「加入购物车」浅红 `#FFECEC` 底红字；「立即购买」品牌红 `#FF2442` 实心白字。两按钮高 40px 全圆角。**不含收藏图标** |

---

#### 购物车页

```
┌──────────────────────────────────────┐
│ 顶部：购物车 + 管理                       │
├──────────────────────────────────────┤
│ 领券结算横条（自动领最优券）             │
├──────────────────────────────────────┤
│ 商品卡片列表（勾选+图+名+规格+步进）      │
├──────────────────────────────────────┤
│ 底部结算栏（全选+合计+去结算）            │
└──────────────────────────────────────┘
```

| 模块 | UI 说明 |
|------|---------|
| 页面背景 | `#F8F8F8` |
| 顶部导航 | 白底，居中「购物车」17px 600，右侧「管理」13px `#333` |
| 领券结算 | 浅红卡片 `#FFF5F5` 圆角 12px，左红「券」标 + 「已自动领取新人券¥5 + 满减券¥10，共省 ¥15」13px `#333` + 右「›」。**核心逻辑**：进入购物车自动领取并**叠加使用**可用券——无门槛券（新人）+ 满减券可同时使用，无需手动操作 |
| 商品列表 | 白卡圆角 16px，卡片间 8px。每条：左圆形勾选框 18px（未选 `#E0E0E0` 描边，选中 `#FF2442` 红填充 + 白勾）。商品图 76px 圆角 12px。商品名 13px + 规格浅灰 pill。底部行：价格 `¥`红 + 数字 15px 700 红，右侧步进器 22px。右下「删除」12px `#C8C8C8` |
| 底部结算栏 | 白底。左全选圆勾 + 「全选」13px。中间合计「合计 ¥142」(¥红 + 数字 18px 700 红)，下方小字「已省 ¥15」`#999` 10px。右「结算(2)」品牌红实心胶囊按钮，高 40px |
| 空状态 | 居中线性空车图标 80px `#E0E0E0` + 「购物车还是空的」13px `#999` + 「去逛逛」红色实心胶囊 |

---

#### 确认订单页

```
┌──────────────────────────────────────┐
│ 顶部：返回 + 确认订单                     │
├──────────────────────────────────────┤
│ 收货地址卡片                              │
├──────────────────────────────────────┤
│ 商品清单卡片                              │
├──────────────────────────────────────┤
│ 优惠券 / 积分抵扣 / 订单备注              │
├──────────────────────────────────────┤
│ 金额明细                                  │
├──────────────────────────────────────┤
│ 底部：合计 + 提交订单                      │
└──────────────────────────────────────┘
```

| 模块 | UI 说明 |
|------|---------|
| 页面背景 | `#F8F8F8`，卡片白底圆角 16px，左右 12px |
| 收货地址 | 白卡 padding 16px。左红色定位图标 18px。姓名 15px 600 + 手机号 15px `#333`，下行地址 13px `#999`。右「›」 |
| 商品清单 | 白卡。每条：商品图 64px 圆角 12px + 名 13px + 规格 pill；右侧价格 `¥`红 13px + 「x1」`#999`。多商品浅灰细线分隔 |
| 优惠券 | 白卡行：左「优惠券」14px，右「已抵 -¥15」红 + 「›」（新人券+满减券自动叠加） |
| 积分抵扣 | 白卡行：左「积分抵扣」14px + 下方小字「当前 1,286 积分，可抵 ¥5」`#999` 11px，右侧**开关 Toggle**（开启为品牌红）。规则：100积分=1元，最高抵订单金额 50%；默认开启用满可抵额度 |
| 订单备注 | 白卡：「备注」14px + 输入框 `#FAFAFA` 底圆角 8px placeholder「选填」`#C8C8C8` |
| 金额明细 | 白卡每行左右对齐，13-14px：商品合计 / **会员折扣**（白银9.8折，金色 -¥3.14）/ 新人券（红 -¥5）/ 满减券（红 -¥10）/ 积分抵扣（红 -¥5）/ 运费。优惠项金额用红色，会员折扣用会员金色 |
| 底部提交栏 | 白底。左「合计 ¥133.86」(¥红 + 20px 700 红) + 小字「已省 ¥23.14」。右「提交订单」品牌红实心胶囊高 42px |

---

#### 支付结果页

```
┌──────────────────────────────────────┐
│ 顶部：完成                                │
├──────────────────────────────────────┤
│ 成功图标 + 「支付成功」                   │
│ 实付金额 + 已省 + 本单可得积分            │
├──────────────────────────────────────┤
│ 查看订单 / 返回首页                       │
└──────────────────────────────────────┘
```

| 模块 | UI 说明 |
|------|---------|
| 页面背景 | 纯白 `#FFFFFF` |
| 成功图标 | 居中偏上。56px 圆形品牌红实心 + 白色对勾，外圈可加极浅红光晕 |
| 成功文字 | 「支付成功」`#1A1A1A` 18px 600，距图标 12px |
| 金额 | 「¥133.86」`#1A1A1A` 26px 700，居中。下方「已为你节省 ¥23.14」红色小胶囊 |
| 可得积分 | 金额下方金色提示「🎁 确认收货后可得 161 积分」（会员金 12px）。说明积分确认收货后发放，白银会员 1.2 倍加速 |
| 操作按钮 | 横向并排两按钮，间距 12px：「查看订单」品牌红实心胶囊；「返回首页」`#F5F5F5` 浅灰底 `#333` 胶囊。各高 40px。**不再展示猜你喜欢推荐区** |

---

#### 订单详情页

```
┌──────────────────────────────────────┐
│ 顶部：返回 + 订单详情                     │
├──────────────────────────────────────┤
│ 订单状态横幅（大字 + 说明 + 可得积分）   │
├──────────────────────────────────────┤
│ 物流时间轴（发货后展示，可展开收起）      │
├──────────────────────────────────────┤
│ 收货地址                                 │
├──────────────────────────────────────┤
│ 商品清单                                 │
├──────────────────────────────────────┤
│ 金额明细（含实付）                        │
├──────────────────────────────────────┤
│ 订单信息（编号/时间/支付方式）            │
├──────────────────────────────────────┤
│ 底部操作栏（按状态变化）                  │
└──────────────────────────────────────┘
```

| 模块 | UI 说明 |
|------|---------|
| 页面背景 | `#F8F8F8`，白卡圆角 16px |
| 订单状态横幅 | 白卡 padding 18px。左侧状态大字 `#1A1A1A` 18px 600 + 下方说明 12px `#999`；右侧状态线性插画 40px。未完成订单底部加金色小条「确认收货后可得 161 积分」 |
| 物流时间轴 | **发货后展示**（待发货状态隐藏）。白卡圆角 16px：标题行「物流信息」+ 右「展开/收起 ›」。展开后为竖向时间轴——左侧圆点 + 竖线（最新节点红色实心点 + 加粗），右侧每节点：物流描述 13px `#333`（如「快件已到达上海转运中心」）+ 时间 11px `#999`（如「06-12 14:30」）。最新状态在最上，默认展示最新 1-2 条，点击展开全部 |
| 收货地址 | 白卡，同确认订单页布局，无右箭头 |
| 商品清单 | 白卡，同确认订单页样式 |
| 金额明细 | 白卡。普通行 13px：商品合计 / 会员折扣（金色 -¥3.14）/ 新人券（红 -¥5）/ 满减券（红 -¥10）/ 积分抵扣（红 -¥5）/ 运费；底部细线后「实付金额」右对齐 `¥`红 + 17px 700 红 |
| 订单信息 | 白卡左右对齐：订单编号（右带「复制」红色小字）/ 下单时间 / 支付方式。标签 13px `#999`，值 13px `#333` |
| 底部操作栏 | 白底按钮右对齐：主操作品牌红实心胶囊（去支付/确认收货），次操作 `#F5F5F5` 浅灰胶囊（取消/退款）。高 36px |

---

#### 个人中心页

```
┌──────────────────────────────────────┐
│ 会员头部卡（合并）：                       │
│   头像 + 昵称 + 会员徽章                  │
│   升级进度条 + 权益标签                   │
├──────────────────────────────────────┤
│ 资产入口：优惠券 / 积分（两等分）         │
├──────────────────────────────────────┤
│ 我的订单：5状态图标（待付/待发/待收/退/完）│
│   └ 下方默认展示最近 3 笔订单预览          │
├──────────────────────────────────────┤
│ 功能菜单（地址管理 / 联系客服 / 关于我们）│
└──────────────────────────────────────┘
```

| 模块 | UI 说明 |
|------|---------|
| 页面背景 | `#F8F8F8` |
| 会员头部卡 | **用户头部 + 会员卡合并为一张金色卡**。会员金渐变（`#E8C97A → #C9A24B`）圆角 16px，左右 12px，内边距 18px，文字白色。第一行：左头像 56px 圆形（2px 白描边）+ 右侧昵称 18px 600 白字 + 紧跟会员徽章 pill「🥈 白银会员」（半透明白底 + 白字 11px）+ 下方「编辑资料 ›」12px 半透明白。第二行：升级进度条（细圆角条，底为半透明白、已填为白）+ 文字「再消费 ¥147 升黄金会员」11px。底部一行权益小标签：「下单 9.8 折」「积分 1.2 倍」半透明白 pill。右上角可放「查看权益 ›」 |
| 资产入口 | 白卡圆角 16px，**两等分**：优惠券 / 积分。每个：数字 22px 700 `#1A1A1A` + 标签 12px `#999`，居中。中间 1px `#F0F0F0` 竖线。点击进入对应页。**已去除收藏** |
| 我的订单 | 白卡圆角 16px。① 标题行「我的订单」15px 600 + 右「全部 ›」`#999`。② 状态入口行：5 个图标横向均分——待付款/待发货/待收货/退款售后/已完成（线性图标 24px + 下方文字 11px `#666`，有未读则右上红点角标），点击进入对应状态的**订单列表页**。③ **最近订单预览**：图标行下方默认展示最近 3 笔订单（无订单则隐藏该区），与图标行用浅灰细线分隔。每条为一行——左侧首件商品图 48px 圆角 8px（多件时右下叠加「共N件」角标）+ 中间商品名 13px 单行省略与状态小字 11px `#999`，右侧实付价 `¥`红 + 数字 14px 700。点击单条进**订单详情页** |
| 功能菜单 | 白卡圆角 16px。**3 行菜单**：地址管理 / 联系客服 / 关于我们。每行：左线性图标 20px + 菜单名 14px，右「›」`#C8C8C8`。行高 48px，浅灰细线分隔。**优惠券 / 积分中心不再在此重复**，统一通过上方「资产入口」点击进入；**已去除「我的收藏」「设置」**；「关于我们」点击进入品牌故事页（数据来源后台「内容管理」） |

---

#### 订单列表页

```
┌──────────────────────────────────────┐
│ 顶部：返回 + 我的订单                     │
├──────────────────────────────────────┤
│ 状态 Tab（全部/待付/待发/待收/售后/完成） │
├──────────────────────────────────────┤
│ 订单卡片列表（按状态筛选）               │
│   订单号·状态 + 商品行 + 实付 + 操作按钮  │
└──────────────────────────────────────┘
```

> 入口：从个人中心「我的订单」的 5 个状态图标或「全部 ›」进入，进入时自动定位到对应状态 Tab；点击预览区单条订单则直接进订单详情页。

| 模块 | UI 说明 |
|------|---------|
| 页面背景 | `#F8F8F8`，白卡圆角 16px，左右 12px |
| 顶部导航 | 高度 48px 白底。左返回箭头 `#1A1A1A`，居中「我的订单」16px 600 |
| 状态 Tab 栏 | 白底吸顶、横向可滑动。6 个 Tab：全部 / 待付款 / 待发货 / 待收货 / 退款售后 / 已完成。未选 `#999` 13px，选中 `#1A1A1A` 14px 600 + 下方 3px 红圆角短指示条（`#FF2442`，宽 16px）。从个人中心点击某状态图标进入时自动选中该 Tab |
| 订单卡片 | 白卡圆角 16px，卡片间 8px。① 卡头行：左「订单号 20240612xxxx」或下单日期 12px `#999`，右侧状态文案（待付款 `#FF2442` 13px、待发货/待收货 `#1A1A1A` 13px、交易完成 `#999` 13px）。② 商品行：可多件，每件左图 64px 圆角 12px + 名 13px 两行省略 + 规格 pill，右侧价 `¥`红 13px + 「x1」`#999`；多件时整单底部合并展示「共 N 件」。③ 金额行：右对齐「实付 ¥133.86」(¥红 + 15px 700 红)。④ 操作行：右对齐按状态变化的胶囊按钮，高 32px——待付款（去支付 红实心 / 取消订单 灰）、待发货（提醒发货 灰）、待收货（确认收货 红实心 / 查看物流 灰）、已完成（再次购买 红实心 / 删除订单 灰）、退款售后（查看进度 灰） |
| 点击 | 点击订单卡商品/空白区进**订单详情页** |
| 空状态 | 居中线性空盒图标 80px `#E0E0E0` + 「暂无相关订单」13px `#999` + 下方「去逛逛」红色实心胶囊 |

---

#### 我的优惠券页

```
┌──────────────────────────────────────┐
│ 顶部：返回 + 我的优惠券                   │
├──────────────────────────────────────┤
│ 状态 Tab（可用 / 已使用 / 已过期）       │
├──────────────────────────────────────┤
│ 券卡列表（左金额 + 右信息 + 去使用）     │
└──────────────────────────────────────┘
```

> 入口：个人中心「资产入口」点击「优惠券」数字进入。

| 模块 | UI 说明 |
|------|---------|
| 页面背景 | `#F8F8F8` |
| 顶部导航 | 高度 48px 白底。左返回箭头 `#1A1A1A`，居中「我的优惠券」16px 600 |
| 状态 Tab | 白底吸顶，3 个 Tab：可用 / 已使用 / 已过期。未选 `#999` 13px，选中 `#1A1A1A` 14px 600 + 下方 3px 红短指示条。「可用」Tab 文字旁带数量 |
| 券卡 | **横向票券造型**，圆角 12px，卡片间 10px，左右 12px。**左侧金额区**（约 100px，红→品牌红渐变底白字）：无门槛券「¥5」大数字 24px 800 + 下方「无门槛」10px；满减券「¥10」+「满100可用」。**右侧信息区**白底：券名 14px 600 `#1A1A1A`（如「新人专享券」「满减券」）+ 有效期 11px `#999`（如「有效期至 2026.06.30」）+ 右下角「去使用」红色实心小胶囊（高 28px）。左右之间用**竖向虚线**分隔（票券质感） |
| 失效态 | 已使用 / 已过期券整卡灰度处理（`grayscale` + 降透明度），左侧金额区转灰，按钮替换为灰描边「已使用」「已过期」文字章 |
| 空状态 | 居中线性券图标 80px `#E0E0E0` + 「暂无可用优惠券」13px `#999` + 下方「去逛逛」红色实心胶囊 |

---

#### 积分中心页

```
┌──────────────────────────────────────┐
│ 金色头部：返回 + 积分中心 + 规则 ›       │
│   当前积分大数字 + 可抵说明 + 加速标签   │
├──────────────────────────────────────┤
│ 积分明细 Tab（全部 / 获取 / 使用）       │
├──────────────────────────────────────┤
│ 积分流水列表（标题 + 时间 + ± 积分）     │
└──────────────────────────────────────┘
```

> 入口：个人中心「资产入口」点击「积分」数字进入。

| 模块 | UI 说明 |
|------|---------|
| 页面背景 | `#F8F8F8` |
| 顶部导航 | 透明叠在金色头部上：左返回箭头白色，居中「积分中心」16px 600 白字，右「规则 ›」12px 半透明白 |
| 积分头部卡 | 会员金渐变（`#E8C97A → #C9A24B`）通栏头部，文字白色居中。中部当前积分大数字 36px 700（如「1,286」）+ 下方「当前可抵 ¥12.86」12px 半透明白。底部金色加速标签「🥈 白银会员积分 1.2 倍加速中」11px（半透明白底 pill） |
| 明细 Tab | 白卡圆角 16px。3 个 Tab：全部 / 获取 / 使用，选中红短指示条（同其他页样式） |
| 积分流水 | 白卡列表，每行：左侧标题 14px `#1A1A1A`（如「下单获得」「积分抵扣」「确认收货奖励」）+ 下方时间 11px `#999`；右侧积分变动——获取为红「+161」15px 700，使用为灰「-500」15px `#999`。浅灰细线分隔 |
| 规则说明 | 点击右上「规则 ›」展示说明：「100 积分 = 1 元」「下单后确认收货发放，白银会员 1.2 倍加速」「最高抵订单金额 50%」等文案 13px `#666` |
| 空状态 | 居中线性图标 80px `#E0E0E0` + 「暂无积分记录」13px `#999` |
