## 后台管理 UI 设计说明

> 视觉基调：**专业高效的中后台风格** —— 信息密度高、结构清晰、操作直达。蓝色主色点睛，深色顶栏稳重，大量留白与浅灰分区，让数据与表格成为主角。区别于 C 端的「种草感」，后台追求「一眼看懂、三步操作完成」。

### 全局设计规范（Design Tokens）

#### 色彩

- **主色蓝** `#1677FF`：主按钮、链接、选中态、关键数据、Tab 高亮，是后台点睛色
- **主色浅底** `#E6F0FF`：选中行 / 浅蓝标签底 / hover 底
- **顶栏深色** `#1A2332`：顶部导航栏背景，深色稳重
- **侧栏背景** `#FFFFFF`：侧边导航白底，选中项浅蓝底 + 蓝左条
- **成功 / 正向** `#52C41A`：上架 / 启用 / 正常 / 运行中状态
- **警告** `#FAAD14`：库存不足等预警
- **危险** `#FF4D4F`：下架 / 禁用 / 删除 / 售罄 / 危险操作
- **主标题** `#262626`：页面标题、表格主文字、指标数字
- **正文** `#595959`：常规文字、单元格
- **次要文字** `#8C8C8C`：副信息、表头辅助、时间
- **占位 / 禁用** `#BFBFBF`：placeholder、未选中态
- **页面背景** `#F0F2F5`：中后台经典灰，衬托白卡
- **卡片 / 内容底** `#FFFFFF`：所有卡片、表格、表单纯白
- **表头底** `#FAFAFA`：表格表头浅灰底
- **边框 / 分隔** `#F0F0F0`：表格线、卡片描边、分隔线

#### 字体

- **页面标题** 20px / 600：各功能页主标题
- **区块 / 卡片标题** 16px / 600：卡片标题、表单分组标题
- **表头** 14px / 500：表格列头
- **正文 / 单元格** 14px / 400：表格内容、表单值
- **辅助 / 时间** 12px / 400：副信息、时间戳、说明
- **指标数字** 28-32px / 700：数据看板大数字
- **字体族** -apple-system / PingFang SC / Microsoft YaHei，无衬线

#### 形状与质感

- **卡片圆角**：**8px**（中后台标准，沉稳不浮夸）
- **按钮 / 输入框圆角**：6px
- **标签 Tag 圆角**：4px
- **阴影**：卡片极轻 `0 1px 2px rgba(0,0,0,.06)`；弹窗 `0 6px 16px rgba(0,0,0,.12)`
- **主按钮**：蓝 `#1677FF` 实心 + 白字 + 500
- **次按钮**：白底 + `#D9D9D9` 边框 + `#595959` 字
- **危险按钮**：红 `#FF4D4F` 实心白字，或红字红边（删除类）
- **文字按钮**：表格操作列常用，蓝色「编辑」、红色「删除」，无边框
- **间距**：基础 8px；卡片内边距 16-24px；页面外边距 24px

#### 通用组件规范

- **表格**：白底，表头 `#FAFAFA` 14px 500，行高 54px，分隔线 `#F0F0F0`，行 hover `#FAFAFA`。操作列为文字按钮（蓝/红），右对齐或独立列
- **状态标签 Tag**：圆角 4px 小标签，浅色底 + 同色字：上架/正常 绿、下架/禁用 灰、售罄/异常 红、库存不足 黄、进行中 蓝
- **工具栏**：表格上方一行：左侧搜索框 + 下拉筛选，右侧主操作按钮（如「+ 新增」）。可附「共 N 条」统计
- **分页器**：卡片右下角，当前页蓝色实心，前后翻页箭头，可选每页条数
- **Tab 选项卡**：下划线式，选中蓝字 + 下方 2px 蓝条；状态筛选 Tab 附数量「待发货(8)」
- **表单**：label 左侧 100px 右对齐 + 右侧控件；分组卡片；底部右对齐「重置」次按钮 +「保存」主按钮；保存成功 Toast
- **拖拽排序**：行首 `≡` 手柄，拖拽调整顺序（分类、Banner）
- **危险二次确认**：删除 / SQL 修改等弹出确认框，显示影响范围后再执行

---

### 后台主框架

```
┌──────────────────────────────────────────────────────────────┐
│ 顶栏（56px，深色 #1A2332）                                       │
│  [≡] [Logo + KOALA 管理后台]              [🔔通知] [👤管理员 ▾]  │
├──────────────┬───────────────────────────────────────────────┤
│ 侧边导航      │ 主内容区（#F0F2F5，可滚动）                      │
│ 220px        │  ┌─ 面包屑：首页 / 商品管理 / 商品列表 ─────────┐ │
│  📊 数据看板  │  │                                            │ │
│  📦 商品管理▾ │  │   嵌入各功能页面（白卡承载）                 │ │
│    · 商品列表 │  │                                            │ │
│    · 分类管理 │  │                                            │ │
│  📋 订单管理  │  │                                            │ │
│  👥 用户管理  │  │                                            │ │
│  🎟️ 优惠券管理│  │                                            │ │
│  🖼️ 内容管理▾ │  │                                            │ │
│    · Banner   │  │                                            │ │
│    · 品牌信息 │  │                                            │ │
│  ⚙️ 系统设置  │  │                                            │ │
│  ──────────  │  │                                            │ │
│  🛠️ 运维管理  │  └────────────────────────────────────────────┘ │
│  v1.0.0      │                                                 │
└──────────────┴───────────────────────────────────────────────┘
```

- **顶栏**：高 56px 深色 `#1A2332`。左：折叠按钮 `≡` + Logo（蓝圆角方）+「KOALA 管理后台」白字 16px。右：通知铃铛（带红点）+ 管理员头像与名称（点击下拉：个人信息 / 退出登录）
- **侧边导航**：宽 220px 白底（折叠 56px）。菜单项高 44px，图标 + 文字 14px；含二级子菜单（商品管理、内容管理可展开）。**选中态**：浅蓝底 `#E6F0FF` + 蓝左条 3px + 蓝字。折叠态仅显示图标，二级以浮层弹出。底部版本号（展开时显示）。运维管理上方有分隔线，**仅超级管理员可见**。**菜单顺序**：数据看板 → 商品管理（商品列表·分类管理）→ 订单管理 → 用户管理 → 优惠券管理 → 营销配置 → 内容管理（Banner·品牌信息）→ 系统设置 → ── → 运维管理
- **主内容区**：背景 `#F0F2F5`，顶部面包屑 14px `#8C8C8C`（末级深色），下方为各功能白卡页面。整体可垂直滚动，内容外边距 24px
- **全局规则**：路由变化时对应菜单自动高亮；折叠动画 200ms；折叠态子菜单 hover 浮层展示

---

### 数据看板

```
┌──────────────────────────────────────────────────────┐
│ 数据看板                          [今日/本周/本月] [刷新]│
├──────────────────────────────────────────────────────┤
│ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐          │
│ │今日订单 │ │今日销售额│ │新增用户 │ │总用户数 │          │
│ │  12    │ │ ¥2,580 │ │   3    │ │  286   │          │
│ │↑20%绿  │ │↑15%绿  │ │↓10%红  │ │        │          │
│ └────────┘ └────────┘ └────────┘ └────────┘          │
│ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐          │
│ │待处理退款│ │今日退款额│ │券核销率 │ │今日客单价│          │
│ │  2 红  │ │ ¥336   │ │  42%   │ │ ¥215   │          │
│ └────────┘ └────────┘ └────────┘ └────────┘          │
├──────────────────────────────────────────────────────┤
│ [销售趋势] [热销商品] [订单分布] [用户分析] [退款分析]    │
├──────────────────────────────────────────────────────┤
│   折线图 / 排行表 / 饼图（按 Tab 切换）                  │
└──────────────────────────────────────────────────────┘
```

- **页面头**：左标题「数据看板」20px 600；右侧时间段切换（今日/本周/本月 胶囊 Tab）+「刷新」次按钮
- **指标卡片（第一行）**：一行 4 张白卡圆角 8px：今日订单 / 今日销售额 / 新增用户 / 总用户数。指标名 14px `#8C8C8C` + 大数字 32px 700 `#262626` + 环比（↑绿 / ↓红 12px）。时间段切换影响全部数据
- **指标卡片（第二行）**：一行 4 张白卡：**待处理退款**（数字红色 `#FF4D4F` 高亮提醒，有待处理时闪烁红点）/ **今日退款额** / **优惠券核销率**（进行中券整体核销率）/ **今日客单价**（当日销售额 / 订单数）。第二行卡片样式与第一行一致，但无环比
- **分析 Tab**：五 Tab：销售趋势（折线图：日期×销售额）/ 热销商品（排行表：商品/销量/销售额 + 查看全部）/ 订单分布（各状态占比饼图）/ 用户分析（新增趋势 + 会员等级分布饼图）/ **退款分析**（退款趋势折线图 + 退款原因分布饼图）。Tab 切换仅换下方内容区
- **交互**：切换时间段、切换 Tab、刷新重载；热销商品「查看全部」跳商品管理；**待处理退款**数字可点击，跳转订单管理并自动切换到「退款售后」Tab

---

### 商品列表

```
┌──────────────────────────────────────────────────────┐
│ 商品列表                                    [+ 新增商品] │
├──────────────────────────────────────────────────────┤
│ [🔍搜索名称] [分类▾] [状态▾]              共 N 件商品    │
├──────────────────────────────────────────────────────┤
│ 主图│商品名称│售价│标签│推荐│库存│分类│状态│操作        │
│ [图]│百花蜜  │¥88│热销│✓ │150│蜂蜜│上架│编辑 下架    │
│ [图]│蜂王浆  │¥168│新品│  │ 8 │蜂蜜│上架│编辑 下架    │
│     │       │   │   │  │⚠不足│   │   │            │
│ [图]│蜂蜡皂  │¥39│   │  │ 0 │日用│售罄│编辑 上架    │
├──────────────────────────────────────────────────────┤
│                              < 1 2 3 >  共3页          │
└──────────────────────────────────────────────────────┘
```

- **页面头**：标题「商品列表」+ 右「+ 新增商品」主按钮（进商品编辑表单）
- **工具栏**：搜索框（商品名）+ 分类下拉 + 状态下拉（上架/下架/售罄）；右侧「共 N 件商品」。筛选实时过滤
- **表格**：列：主图 48px / 名称 / 售价（红 ¥）/ 标签 Tag（热销/新品/礼盒/特惠，可多）/ 推荐（✓）/ 库存 / 分类 / 状态 Tag / 操作。**库存 < 20 黄色「库存不足」预警，= 0 红色「已售罄」**。操作列：编辑（蓝）、上/下架（切换）
- **商品字段**：名称、主图、售价、规格（最多 4）、标签（多选）、是否推荐（用于 C 端精选推荐）、库存、分类、详情图、产品亮点
- **营销参与（编辑表单）**：商品编辑表单内**无营销开关**，所有商品默认统一参与满减券与无门槛券。**会员无下单折扣，亦无任何商品营销开关**
- **分页**：右下角分页器

---

### 分类管理

```
┌──────────────────────────────────────────────────────┐
│ 分类管理                                    [+ 新增分类] │
├──────────────────────────────────────────────────────┤
│ 排序│图标│分类名称│商品数│状态│操作                     │
│ ≡1 │🍯 │蜂蜜   │ 12 │启用│编辑 禁用                  │
│ ≡2 │🧴 │日用   │  5 │启用│编辑 禁用                  │
│ ≡3 │🎁 │礼盒   │  3 │启用│编辑 禁用                  │
│ ≡4 │🌿 │养生   │  0 │禁用│编辑 启用 删除             │
└──────────────────────────────────────────────────────┘
```

- **页面头**：标题 + 右「+ 新增分类」主按钮（弹窗：分类名 + 上传图标）
- **表格**：列：排序（`≡` 拖拽手柄）/ 图标 / 名称 / 商品数 / 状态 Tag / 操作。拖拽 `≡` 调整顺序
- **规则**：同步到 C 端首页品类专区与列表筛选栏；**仅商品数为 0 的分类可删除**；禁用分类 C 端不展示

---

### 订单管理

```
┌──────────────────────────────────────────────────────┐
│ 订单管理                                      [导出订单] │
├──────────────────────────────────────────────────────┤
│ [全部86][待付款5][待发货8][待收货12][已完成56][已取消3][退款售后2]│
├──────────────────────────────────────────────────────┤
│ [🔍搜索订单号/买家]                                     │
├──────────────────────────────────────────────────────┤
│ 订单号│买家│商品│金额│状态│下单时间│操作                │
│ ...01│张三│百花蜜│¥78│待发货│06-10│详情 发货         │
│  ▼展开：收货人/地址/商品明细/优惠券/实付                 │
│ ...98│李四│蜂王浆│¥168│退款售后│06-09│详情 处理退款    │
├──────────────────────────────────────────────────────┤
│                              < 1 2 3 >                 │
└──────────────────────────────────────────────────────┘
```

- **页面头**：标题 + 右「导出订单」次按钮（导出当前筛选结果）
- **状态 Tab**：全部 / 待付款 / 待发货 / 待收货 / 已完成 / 已取消 / 退款售后，各附数量。蓝色下划线选中
- **搜索**：订单号 / 买家姓名
- **表格**：列：订单号 / 买家 / 商品（首件+共N件）/ 金额（红）/ 状态 Tag / 下单时间 / 操作。点击行展开详情（收货人、地址、商品明细、优惠券、运费、实付、**订单备注**）。「发货」仅待发货显示；「处理退款」仅退款售后显示。**退款售后状态子标签**：待审核 蓝 / 已同意·待寄回 黄 / 已寄回·待确认 黄 / 已退款 绿 / 已拒绝 红
- **发货**：点击「发货」弹窗填写**物流公司 + 物流单号** → 状态转待收货，单号同步 C 端物流时间轴
- **处理退款**：点击「处理退款」弹出**售后处理弹窗**（宽 520px）：① 顶部展示买家申请信息——退款类型（仅退款/退货退款）、退款金额（红色大字）、退款原因、补充说明、凭证图片（可点击放大）；② 底部操作区——「同意」蓝色主按钮 +「拒绝」红色危险按钮；拒绝时展开文本框填写拒绝理由（必填）。**仅退款（未发货）**：同意后系统自动原路退款。**退货退款（已发货）**：同意后等待买家寄回 → 订单展示寄回物流单号 → 操作列变为「确认收货」蓝色按钮 → 点击弹出二次确认「确认已收到退回商品？确认后将自动退款」→ 确认后系统原路退款。退款时现金原路退、未过期券退回
- **订单详情展开**：展开行增加**订单备注**字段（若用户填写了备注则展示，未填写显示「—」），位于实付金额下方

---

### 用户管理

```
┌──────────────────────────────────────────────────────┐
│ 用户管理                                                │
├──────────────────────────────────────────────────────┤
│ [🔍搜索昵称/手机号]                       共 N 位用户    │
├──────────────────────────────────────────────────────┤
│ 头像│昵称│手机号│会员│累计消费│订单数│注册时间│状态│操作 │
│ [头]│张三│138**│🥈白银│¥2,580│15│01-15│正常│详情 禁用│
│ [头]│李四│139**│普通 │¥120 │ 3│03-20│正常│详情 禁用│
├──────────────────────────────────────────────────────┤
│                              < 1 2 3 >                 │
└──────────────────────────────────────────────────────┘
```

- **工具栏**：搜索（昵称 / 手机号）+「共 N 位用户」
- **表格**：列：头像 / 昵称 / 手机号 / **会员等级 Tag** / 累计消费 / 订单数 / 注册时间 / 状态 / 操作。会员等级由累计消费自动判定（白银≥500、黄金≥1500），**权益为升级送券**，门槛与礼券可在「营销配置」中调整
- **详情**：点击「详情」进入**用户详情页**（独立页面，面包屑「用户管理 / 用户详情」）
- **禁用**：切换用户状态，**禁用后无法登录商城**

---

#### 用户详情页

```
┌──────────────────────────────────────────────────────┐
│ ‹ 返回用户列表                                          │
├──────────────────────────────────────────────────────┤
│ ┌── 用户信息 ────────────────────────────────────┐    │
│ │ [头像] 张三  138****1234  🥈白银会员              │    │
│ │ 累计消费 ¥2,580 │ 订单 15 笔                       │    │
│ │ 注册时间 2026-01-15  状态 ●正常                   │    │
│ └──────────────────────────────────────────────────┘    │
│ [消费记录] [优惠券]                                       │
├──────────────────────────────────────────────────────┤
│   订单列表 / 券列表（按 Tab 切换）                        │
└──────────────────────────────────────────────────────┘
```

- **用户信息卡**：白卡圆角 8px。左侧头像 56px 圆形 + 昵称 18px 600 + 手机号 14px `#8C8C8C` + 会员等级 Tag。下方一行统计：累计消费 / 订单数，数字 20px 700 `#262626`。底部注册时间 + 状态标签
- **消费记录 Tab**：该用户的订单列表（简化版）：订单号 / 商品（首件+共N件）/ 金额 / 状态 Tag / 下单时间。点击订单号可跳转订单管理详情
- **优惠券 Tab**：该用户持有的券列表：券名 / 类型（满减/无门槛）/ 来源（新人 / 升级礼券 / 活动）/ 状态 Tag（可用 绿 / 已使用 灰 / 已过期 灰）/ 使用时间（已使用时展示）/ 有效期

---

### 优惠券管理

```
┌──────────────────────────────────────────────────────┐
│ 优惠券管理                                  [+ 新建优惠券]│
├──────────────────────────────────────────────────────┤
│ [全部][进行中][未开始][已结束][已停发]                   │
├──────────────────────────────────────────────────────┤
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐      │
│ │🎟️满100减10   │ │🎟️新人立减5  │ │🎟️满200减25   │      │
│ │满减券        │ │无门槛券      │ │满减券        │      │
│ │06.01~06.30   │ │长期有效      │ │06.15~07.15   │      │
│ │限领2张/人    │ │限领1张/人   │ │限领1张/人    │      │
│ │已领156/500   │ │已领89/200   │ │已领0/300     │      │
│ │已用42 核销27%│ │已用67 核销75%│ │未开始        │      │
│ │[编辑][停发]  │ │[编辑][停发] │ │[编辑][删除]  │      │
│ │●进行中绿     │ │●进行中绿    │ │○未开始灰     │      │
│ └─────────────┘ └─────────────┘ └─────────────┘      │
└──────────────────────────────────────────────────────┘
```

- **页面头**：标题 + 右「+ 新建优惠券」主按钮
- **状态 Tab**：全部 / 进行中 / 未开始 / 已结束 / 已停发
- **券卡（三列网格）**：每卡：券名 + 类型 + 有效期 + 限领 + **已领 X/总量** + **已用 + 核销率** + 状态点（进行中绿/未开始灰/已结束·已停发）+ 操作（编辑 / 停发 / 删除）
- **新建字段**：券名、券类型（满减 / 无门槛）、优惠金额（满减额外填门槛）、有效期、每人限领张数、发行总量
- **规则**：进行中的券同步 C 端商品卡券标（直写「减10」/「领5元」）与详情页领券入口；**已开始的券不可删除，只能停发**
- **购物车凑单开关**：已移至「营销配置」模块统一管理

---

### 营销配置

```
┌──────────────────────────────────────────────────────┐
│ 营销配置                                                │
│ [会员等级与升级礼券] [购物车凑单]                         │
├──────────────────────────────────────────────────────┤
│ ── 会员等级与升级礼券 ──                                 │
│ 等级│升级门槛│升级礼券│当前人数│操作                     │
│ 🥉普通│注册即享│无│ 180 │ —                            │
│ 🥈白银│≥500元│5元券×1 + 满200减20×1│ 86 │ 编辑          │
│ 🥇黄金│≥1500元│10元券×2 + 满300减50×1│ 20 │ 编辑        │
│                                                        │
│ ── 购物车自动凑单 ──                                     │
│ [○关闭] 商家手动控制优惠成本                              │
│                                       [重置] [保存]     │
└──────────────────────────────────────────────────────┘
```

- **页面头**：标题「营销配置」20px 600
- **二 Tab**：会员等级与升级礼券 / 购物车凑单，蓝色下划线选中
- **会员等级与升级礼券 Tab**：表格展示 3 个等级：等级名（含图标）/ 升级门槛 / **升级礼券** / **当前人数**（实时统计）/ 操作。普通会员不可编辑（注册即享、无礼券）；白银、黄金可点击「编辑」弹窗修改**门槛金额** + **升级礼券配置**（可配多张券，每张含：券类型「满减/无门槛」、面额、满减门槛、张数）。修改后实时生效，已有用户按新门槛重新判定等级。**会员无下单折扣、无积分**，权益完全体现为升级时一次性发放的优惠券
- **购物车凑单 Tab**：开关 Toggle + 说明文案。关闭态：灰色 Toggle +「商家手动控制优惠成本」说明。开启态：蓝色 Toggle +「系统自动叠加最优券组合」说明
- **底部**：「重置」次按钮 +「保存」主按钮，保存成功 Toast

---

### Banner 管理

```
┌──────────────────────────────────────────────────────┐
│ Banner 管理                                [+ 新增Banner]│
├──────────────────────────────────────────────────────┤
│ 排序│预览图│跳转链接│状态│操作                          │
│ ≡1 │[图] │/list?..│上线│编辑 下线                     │
│ ≡2 │[图] │/list?..│上线│编辑 下线                     │
│ ≡3 │[图] │/list?..│下线│编辑 上线 删除                │
└──────────────────────────────────────────────────────┘
```

- **页面头**：标题 + 右「+ 新增 Banner」主按钮（上传图片 + 跳转链接 + 状态）
- **表格**：列：排序（`≡` 拖拽）/ 预览图 / 跳转链接 / 状态 Tag / 操作
- **规则**：同步 C 端首页 Banner 轮播区（**纯图轮播，不叠加标签**）；**仅上线状态在 C 端展示**；下线可删除，删除后序号自动重排

---

### 品牌信息

```
┌──────────────────────────────────────────────────────┐
│ 品牌信息                                                │
│ ┌── 基本信息 ──────────────────────────────────────┐  │
│ │ 品牌名称：[ KOALA 优选 ]                           │  │
│ │ App 图标：[上传][预览]   Logo：[上传][预览]         │  │
│ └──────────────────────────────────────────────────┘  │
│ ┌── 品牌故事 ──────────────────────────────────────┐  │
│ │ 标题：[ 关于 KOALA ]                               │  │
│ │ 封面图：[上传][预览]                                │  │
│ │ 简介（≤200字）：[ 纯文本输入框 ]                    │  │
│ └──────────────────────────────────────────────────┘  │
│                                       [重置] [保存]     │
└──────────────────────────────────────────────────────┘
```

- **基本信息**：品牌名称输入、App 图标 / Logo 上传（带预览）
- **品牌故事**：标题、封面图、纯文本简介（限 200 字）
- **底部**：「重置」次按钮 +「保存」主按钮，保存成功 Toast
- **规则**：品牌名称同步 C 端顶部导航与页面标题；品牌故事同步个人中心「关于我们」品牌故事页；图片支持预览

---

### 系统设置

```
┌──────────────────────────────────────────────────────┐
│ 系统设置                                                │
│ [配送设置] [支付设置]                                    │
├──────────────────────────────────────────────────────┤
│ ── 配送设置 ──                                          │
│ 基础运费：[ 8 ] 元                                       │
│ 满额包邮：满 [ 99 ] 元包邮                               │
│                                                        │
│ ── 支付设置 ──                                          │
│ 微信支付：[✓已启用]                                     │
│ 商户号：[**********]   API密钥：[**********]            │
│                                       [重置] [保存]     │
└──────────────────────────────────────────────────────┘
```

- **选项卡**：配送设置 / 支付设置 / **订单设置** 三 Tab
- **配送设置**：基础运费、满额包邮门槛（同步 C 端运费计算与包邮判断）
- **支付设置**：微信支付开关 Toggle + 商户号 + API 密钥（脱敏展示）
- **订单设置**：**待付款超时时间**：输入框 + 「分钟」，默认 30，范围 10~120（同步 C 端待付款倒计时）。**自动确认收货**：输入框 + 「天」，默认 15，发货后 N 天自动确认收货
- **底部**：「重置」+「保存」，保存成功 Toast

---

### 运维管理（仅超级管理员）

```
┌──────────────────────────────────────────────────────┐
│ 运维管理                                                │
│ [系统状态] [操作日志] [数据操作]                          │
├──────────────────────────────────────────────────────┤
│ ── 系统状态 ──                                          │
│ ┌服务●运行中┐┌数据库●正常┐┌Redis●正常┐┌磁盘 35%┐      │
│ JVM：堆256M/512M | CPU 12% | 内存1.2G/2G               │
│ ── 操作日志 ──                                          │
│ [🔍操作人/内容][类型▾][日期]                             │
│ 时间│操作人│类型│内容                                   │
│ ── 数据操作 ──                                          │
│ SQL查询(只读)[执行查询] | ⚠SQL修改[执行修改·二次确认]    │
└──────────────────────────────────────────────────────┘
```

- **访问控制**：**仅 super_admin 角色可访问**，侧栏分隔线下方独立
- **系统状态**：服务 / 数据库 / Redis / 磁盘状态卡（绿点正常）+ JVM 内存、CPU、负载监控，自动刷新
- **操作日志**：全部写操作记录（时间 / 操作人 / 类型 / 内容），支持搜索 / 类型筛选 / 日期范围 / 分页
- **数据操作**：**SQL 查询（只读）**直接执行；**SQL 修改需二次确认**（弹框显示 SQL + 影响行数预估）。**禁止 DROP/TRUNCATE 等危险语句**；查询结果最多 500 行；所有数据操作记入操作日志
